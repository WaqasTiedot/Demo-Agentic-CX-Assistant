services:
  - type: web
    name: agentic-cx-assistant
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"

ANTHROPIC_API_KEY=your_key_here